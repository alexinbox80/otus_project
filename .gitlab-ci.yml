before_script:
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$SSH_PRIVATE_KEY" | base64 -d)
    - mkdir -p ~/.ssh
    - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config

deploy_server1:
    stage: deploy
    environment:
        name: server1
        url: $SERVER1
    script:
        - ssh -p 2222 $SSH_USER@$SERVER1 "sudo rm -rf /var/www/demo &&
          cd /var/www &&
          git clone -b lesson-24 https://github.com/alexinbox80/otus_project.git demo &&
          sudo chown www-data:www-data demo -R &&
          cd demo &&
          sh ./deploy/deploy.sh $SERVER1 $DATABASE_HOST $DATABASE_USER $DATABASE_PASSWORD $DATABASE_NAME $RABBITMQ_HOST $RABBITMQ_USER $RABBITMQ_PASSWORD"
    only:
        - master
